data:
  train_data_dir: data/train
  test_data_dir: data/test
  val_data_dir: data/val
  train_batch_size: 128
  test_batch_size: 32
  predict_batch_size: 256
model:
  num_classes: 3
logging:
  model_name: rps_efficientnet
trainer:
  accelerator: mps
  devices: 1
num_epochs: 1
output_file: rps_model_${data.train_batch_size}_${model.num_classes}.pth
